# Review Journal

The cases below are the review handles I would use before changing the implementation.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its graphics focus without claiming live deployment or external usage.

## Cases

- `baseline`: `geometry span`, score 247, lane `ship`
- `stress`: `atlas pressure`, score 128, lane `watch`
- `edge`: `shader drift`, score 166, lane `ship`
- `recovery`: `render budget`, score 222, lane `ship`
- `stale`: `geometry span`, score 112, lane `watch`

## Note

The repository should be understandable without pretending it is larger than it is.
